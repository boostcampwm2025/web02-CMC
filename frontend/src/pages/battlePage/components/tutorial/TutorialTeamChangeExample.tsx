import TeamChangeModalContent from '../modals/TeamChangeModalContent';

export default function TutorialTeamChangeExample() {
  return <TeamChangeModalContent currentTeam="A" teamACount={15} teamBCount={12} noneCount={3} remainingTime="02:30" />;
}
